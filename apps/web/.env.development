# Local development only — `next dev` loads this automatically.
# Keeps the historical local default (direct FastAPI on port 8000) now that
# the build-time fallback in next.config.ts is the same-origin "/api/v1"
# proxy used by the Vercel deployment.
NEXT_PUBLIC_API_URL=http://localhost:8000

{
  "name": "vibegpt-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker API gateway for VibeGPT (rate limiting, auth gate, origin token, security headers).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260811.1",
    "typescript": "^5.9.0",
    "wrangler": "^4.36.0"
  }
}
